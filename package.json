{
  "scripts": {
    "format": "prettier --write .",
    "format-check": "prettier --check .",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "prettier": "^3.4.1"
  },
  "dependencies": {
    "path": "^0.12.7",
    "readline": "^1.3.0"
  }
}
